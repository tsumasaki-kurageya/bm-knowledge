# AGENTS.md

このファイルは、AI エージェントが `bm-knowledge` を調査・編集するときのルールです。リポジトリ全体に適用します。

## 目的と対象

- 建物維持保全を成立させる業務を、主体横断で整理する。
- 特定のビルメンテナンス会社の組織や担当範囲を、業務体系そのものとして扱わない。
- 実施主体、依頼主体、承認主体、確認主体などは、Domain や Process の属性・関係として記録する。

## 記述ルール

1. 推測を事実として記載しない。
2. 標準・法令・公的資料等に基づく一般知識と、特定現場で観察した実態を分離する。
3. 情報源を可能な限り記録し、URL、資料名、発行主体、参照日など再確認に必要な情報を残す。
4. 情報が不足している場合は補完せず、「未確認事項」に具体的な確認点を残す。
5. Evidence では「観察した事実」と「解釈」を別の見出しに書く。
6. 断定の強さを根拠に合わせ、Frontmatter の `confidence` に反映する。
7. 既存 Markdown を検索し、重複、用語の揺れ、内容の矛盾を確認してから追加・変更する。
8. 既存の永続 ID を変更または再利用しない。

## 情報源と根拠

- 法令、告示、行政資料、規格、原典を優先する。
- 二次資料を使う場合は、発行主体と原典との関係を明記する。
- Evidence の参照先は、可能な限り文書の永続 ID で示す。
- 情報源が確認できない知識は、`confidence: low` とし、未確認事項へ確認方法を記載する。
- 法令や制度など更新される情報には参照日を記録する。

## 標準知識・Evidence・Insight の分離

- `docs/00-business/`、`docs/01-domains/`、`docs/02-processes/` には一般化した業務知識を置く。
- `docs/03-evidence/` には現場で観察・取得した生の事実を置く。
- `docs/04-insights/` には Evidence を根拠とする差分、課題、仮説、機会を置く。
- 1件の Evidence から一般化しない。一般化する場合は、根拠範囲と不確実性を明示する。

## 機密情報

- `docs/03-evidence/information-management.md` に従う。
- 必要以上の顧客名、物件名、個人名、連絡先、認証情報、契約上の秘密を記録しない。
- 匿名化しても再特定できる組み合わせに注意する。
- 保存してよいか判断できない情報はコミットせず、未確認事項として保存可否を確認する。

## Frontmatter とリンク

- 新規文書は `templates/` から作成し、`schemas/frontmatter.md` に従う。
- ID は分類ごとの接頭辞と3桁以上の連番を用いる。
- 関連文書はファイルパスだけでなく永続 ID も併記する。
- ファイルを移動・改名した場合は、リポジトリ内の参照を更新する。

## 変更手順

1. 対象 Issue と完了条件を読む。
2. `rg` などで関連文書、用語、ID を検索する。
3. Issue 単位のブランチで編集する。
4. 差分を読み直し、事実と解釈、情報源、未確認事項、機密性を確認する。
5. Pull Request に変更理由、変更内容、確認内容、対象 Issue を記載する。

`main` へ直接変更しません。1つの Pull Request には、原則として1つのテーマだけを含めます。
